<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gaoyuan.materialdesign">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".recycler.RecyclerViewActivity" />
        <activity android:name=".DrawerLayoutActivity" />
        <activity android:name=".NavigationActivity" />
        <activity android:name=".SnackBarActivity" />
        <activity android:name=".TextInputLayoutActivity" />
        <activity android:name=".ToolBarActivity" />
        <activity android:name=".SearchViewActivity" />
        <activity android:name=".scroll_toorbar.TranslucentScrollToolbarActivity" />
        <activity android:name=".LinearLayoutCompatActivity" />
        <activity android:name=".PaletteActivity" />
        <activity android:name=".TranslucentActivity"></activity>
        <activity
            android:configChanges="orientation|screenSize"
            android:name="me.nereo.multi_image_selector.MultiImageSelectorActivity" />
    </application>

</manifest>